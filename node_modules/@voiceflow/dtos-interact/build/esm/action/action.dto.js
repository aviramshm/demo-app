import { z } from 'zod';
import { ActionType } from './action-type.enum.js';
export const BaseActionDTO = z
    .object({
    type: z.string(),
    payload: z.unknown(),
})
    .passthrough();
export const OpenURLActionDTO = BaseActionDTO.extend({
    type: z.literal(ActionType.OPEN_URL),
    payload: z
        .object({
        url: z.string(),
    })
        .passthrough(),
}).passthrough();
//# sourceMappingURL=action.dto.js.map