import { z } from 'zod';
import { RequestType } from './request-type.enum.js';
import { BaseRequestDTO } from './utils.dto.js';
export const NoReplyRequestDTO = BaseRequestDTO.omit({ payload: true })
    .extend({
    type: z.literal(RequestType.NO_REPLY),
})
    .passthrough();
export const isNoReplyRequest = (value) => NoReplyRequestDTO.safeParse(value).success;
//# sourceMappingURL=no-reply-request.dto.js.map