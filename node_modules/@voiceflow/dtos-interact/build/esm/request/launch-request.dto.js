import { z } from 'zod';
import { RequestType } from './request-type.enum.js';
import { BaseRequestDTO } from './utils.dto.js';
export const LaunchRequestDTO = BaseRequestDTO.extend({
    type: z.literal(RequestType.LAUNCH),
    payload: z
        .object({
        persona: z.string().optional(),
    })
        .passthrough()
        .optional(),
}).passthrough();
export const isLaunchRequest = (value) => LaunchRequestDTO.safeParse(value).success;
//# sourceMappingURL=launch-request.dto.js.map