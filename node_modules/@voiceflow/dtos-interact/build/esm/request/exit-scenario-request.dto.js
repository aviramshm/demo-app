import { z } from 'zod';
import { RequestType } from './request-type.enum.js';
import { BaseRequestDTO } from './utils.dto.js';
export const ExitScenarioRequestDTO = BaseRequestDTO.omit({ payload: true })
    .extend({
    type: z.literal(RequestType.EXIT_SCENARIO),
    payload: z.object({
        intentID: z.string(),
    }),
})
    .passthrough();
export const isExitScenarioRequest = (value) => ExitScenarioRequestDTO.safeParse(value).success;
//# sourceMappingURL=exit-scenario-request.dto.js.map