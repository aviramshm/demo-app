import { z } from 'zod';
import { RequestType } from './request-type.enum.js';
import { BaseRequestDTO } from './utils.dto.js';
export const TextRequestDTO = BaseRequestDTO.extend({
    type: z.literal(RequestType.TEXT),
    payload: z.string(),
}).passthrough();
export const isTextRequest = (value) => TextRequestDTO.safeParse(value).success;
//# sourceMappingURL=text-request.dto.js.map