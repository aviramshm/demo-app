import { z } from 'zod';
import { ActionAndLabelRequestPayloadDTO } from '../payload.dto.js';
import { GeneralRequestDTO } from './general-request.dto.js';
export const PathRequestDTO = GeneralRequestDTO.extend({
    type: z.string().refine((val) => val.startsWith('path-')),
    payload: ActionAndLabelRequestPayloadDTO.passthrough().required({ label: true }),
}).passthrough();
export const isPathRequest = (request) => PathRequestDTO.safeParse(request).success;
//# sourceMappingURL=path-request.dto.js.map