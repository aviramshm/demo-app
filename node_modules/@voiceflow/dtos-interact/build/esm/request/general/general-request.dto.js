import { z } from 'zod';
import { ActionAndLabelRequestPayloadDTO } from '../payload.dto.js';
import { RequestType } from '../request-type.enum.js';
import { BaseRequestDTO } from '../utils.dto.js';
export const GeneralRequestDTO = BaseRequestDTO.extend({
    type: z.string().refine((val) => !Object.values(RequestType).includes(val)),
    payload: ActionAndLabelRequestPayloadDTO.passthrough().optional(),
}).passthrough();
export const isGeneralRequest = (value) => GeneralRequestDTO.safeParse(value).success;
//# sourceMappingURL=general-request.dto.js.map