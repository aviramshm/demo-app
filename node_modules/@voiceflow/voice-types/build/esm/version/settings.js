import { prompt } from '../utils/index.js';
import { BaseVersion } from '@voiceflow/base-types';
export const defaultSettings = ({ error, defaultVoice = null, ...baseSettings }, { defaultPromptVoice }) => ({
    ...BaseVersion.defaultSettings(baseSettings),
    error: prompt.defaultPrompt(error, defaultVoice ?? defaultPromptVoice),
    defaultVoice,
});
