export const defaultPrompt = (prompt, defaultVoice) => {
    if (!prompt?.content) {
        return null;
    }
    return {
        voice: prompt.voice || defaultVoice,
        content: prompt.content,
    };
};
