import { PropsWithChildren } from 'react';
import { DevToolOptions } from './common';
declare const Tools: ({ children, module, console, redux, navigation, variants }: PropsWithChildren<DevToolOptions>) => JSX.Element;
export default Tools;
