import React, { PropsWithChildren } from 'react';
import { StyleProp, ViewStyle } from 'react-native';
import { ReduxStore } from './devtools/common';
interface Props {
    style?: StyleProp<ViewStyle>;
    debug?: boolean | {
        network?: boolean;
        console?: boolean;
    };
    reduxStore?: ReduxStore;
}
export declare const Providers: {
    (): JSX.Element;
    $$NAME: string;
};
export declare const Application: React.MemoExoticComponent<{
    ({ children, style, debug, reduxStore }: PropsWithChildren<Props>): JSX.Element;
    defaultProps: {
        debug: boolean;
        style: {
            flex: number;
        };
    };
}>;
export {};
